{
  "name": "@claude-worker/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bare demo consumer proving the embeddable claim: a Vite + React app that creates sessions against a local claude-worker server and mounts the panel. Run the server with `pnpm server`, then `pnpm demo`.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsgo -p tsconfig.json"
  },
  "dependencies": {
    "@claude-worker/client": "workspace:*",
    "@claude-worker/protocol": "workspace:*",
    "@claude-worker/react": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react-swc": "^3.9.0",
    "vite": "^6.3.0"
  }
}
